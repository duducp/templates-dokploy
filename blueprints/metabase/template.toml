[variables]
main_domain = "${domain}"
postgres_password = "${password:32}"

[config]
env = [
    "POSTGRES_PASSWORD=${postgres_password}"
]
mounts = []

[[config.domains]]
serviceName = "metabase"
port = 3_000
host = "${main_domain}"
